"""The script: template library, condition DSL, scheduler, renderer.

The operator writes every sentence. This package only decides which of his
sentences is appropriate right now, and fills the numbers in.
"""
